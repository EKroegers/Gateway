export interface Props {
  // String that labels the icon
  caption: string;
  // ID That is used to connect the Icon to another interactable element
  connectionID: number;
  // String that handles the file path to the image that the icon will display
  imgSrc: string;
}
